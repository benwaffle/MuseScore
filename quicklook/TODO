- setup codesigning in dev with entitlements, and also for prod
- does the QL plugin load if you just open the app in dev? yes

qlmanage -m is a lie
pluginkit is where it's at

sudo log stream --predicate 'process == "MuseScoreQLPreviewProvider"'
